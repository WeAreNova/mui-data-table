name: Pull Request
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v2

      - run: npm i -g npm@8

      - name: Install
        run: npm ci

      - name: Test
        run: npm run test
